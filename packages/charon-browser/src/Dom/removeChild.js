/**
 * Removes a child node from the DOM and returns the removed node
 *
 * @param {HTMLElement} parentElement
 * @param {HTMLElement} element
 * @returns {HTMLElement}
 */
export const removeChild = (parentElement, element) => parentElement.removeChild(element);