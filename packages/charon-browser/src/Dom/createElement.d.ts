/**
 * creates the HTML element specified by tagName
 *
 * @param tagName - A string that specifies the type of element to be created
 */
export function createElement(tagName: string): HTMLElement | HTMLUnknownElement;