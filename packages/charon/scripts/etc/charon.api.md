## API Report File for "@produck/charon"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

// @public
function abs(x: number): number;

// @public
function acos(x: number): number;

// @public
function acosh(x: number): number;

// @public
function apply(target: Function, thisArgument: any, argumentsList: ArrayLike<any>): any;

// @public
function asin(x: number): number;

// @public
function asinh(x: number): number;

// @public
function assign<T, U>(target: T, source: U): T & U;

// @public
function assign<T, U, V>(target: T, source1: U, source2: V): T & U & V;

// @public
function assign<T, U, V, W>(target: T, source1: U, source2: V, source3: W): T & U & V & W;

// @public
function assign(target: object, ...sources: any[]): any;

// @public
function atan(x: number): number;

// @public
function atan2(y: number, x: number): number;

// @public
function atan2xy(x: number, y: number): number

// @public
function atanh(x: number): number;

// @public
function cbrt(x: number): number;

// @public
function ceil(x: number): number;

// @public
function clz32(x: number): number;

declare namespace Console_2 {
    export {
        log,
        warn,
        error
    }
}
export { Console_2 as Console }

// @public
function construct(target: Function, argumentsList: ArrayLike<any>, newTarget?: Function): any;

// @public
function cos(x: number): number;

// @public
function cosh(x: number): number;

// @public
function create(o: object | null): any;

// @public
function create(o: object | null, properties: PropertyDescriptorMap & ThisType<any>): any;

// @public
function defineProperties<T>(o: T, properties: PropertyDescriptorMap & ThisType<any>): T;

// @public
function defineProperty<T>(o: T, p: PropertyKey, attributes: PropertyDescriptor & ThisType<any>): T;

// @public
function defineProperty_2(target: object, propertyKey: PropertyKey, attributes: PropertyDescriptor): boolean;

// @public
function deleteProperty(target: object, propertyKey: PropertyKey): boolean;

// @public
const E: number;

// @public
function entries<T>(o: { [s: string]: T } | ArrayLike<T>): [string, T][];

// @public
function entries(o: {}): [string, any][];

// @public
function error(message?: any, ...optionalParams: any[]): void;

// @public
function exp(x: number): number;

// @public
function expm1(x: number): number;

// @public
function floor(x: number): number;

// @public
function freeze<T>(a: T[]): readonly T[];

// @public
function freeze<T extends Function>(f: T): T;

// @public
function freeze<T>(o: T): Readonly<T>;

// @public
function fromEntries<T = any>(entries: Iterable<readonly [PropertyKey, T]>): { [k: string]: T };

// @public
function fromEntries(entries: Iterable<readonly any[]>): any;

// @public
function fround(x: number): number;

// @public
function get(target: object, propertyKey: PropertyKey, receiver?: any): any;

// @public
function getOwnPropertyDescriptor(o: any, p: PropertyKey): PropertyDescriptor | undefined;

// @public
function getOwnPropertyDescriptor_2(target: object, propertyKey: PropertyKey): PropertyDescriptor | undefined;

// @public
function getOwnPropertyDescriptors<T>(o: T): {[P in keyof T]: TypedPropertyDescriptor<T[P]>} & { [x: string]: PropertyDescriptor };

// @public
function getOwnPropertyNames(o: any): string[];

// @public
function getOwnPropertySymbols(o: any): symbol[];

// @public
function getPrototypeOf(o: any): any;

// @public
function getPrototypeOf_2(target: object): object | null;

// @public
function has(target: object, propertyKey: PropertyKey): boolean;

// @public
function hypot(...values: number[]): number;

// @public
function imul(x: number, y: number): number;

// @public
function instanceOf(object: object, constructor: Function): boolean;

declare namespace Is {
    export {
        isArray as Array,
        isBoolean as Boolean,
        isNaN_2 as NaN,
        isNull as Null,
        isNumber as Number,
        isObject as Object,
        isString as String,
        isUndefined as Undefined,
        isFunction as Function,
        isSymbol as Symbol,
        isBigInt as BigInt,
        isDate as Date,
        isRegExp as RegExp,
        isArrayBuffer as ArrayBuffer
    }
}

// @public
function is(value1: any, value2: any): boolean;

// @public
function isArray(any: any): boolean;

// @public
function isArrayBuffer(operand: any): boolean;

// @public
function isBigInt(operand: any): boolean;

// @public
function isBoolean(operand: any): boolean;

// @public
function isDate(operand: any): boolean;

// @public
function isExtensible(o: any): boolean;

// @public
function isExtensible_2(target: object): boolean;

// @public
function isFrozen(o: any): boolean;

// @public
function isFunction(operand: any): boolean;

// @public
function isNaN_2(any: any): boolean;

// @public
function isNotArray(operand: any): boolean;

// @public
function isNotArrayBuffer(operand: any): boolean;

// @public
function isNotBigInt(operand: any): boolean;

// @public
function isNotBoolean(operand: any): boolean;

// @public
function isNotDate(operand: any): boolean;

// @public
function isNotFunction(operand: any): boolean;

// @public
function isNotNaN(operand: any): boolean;

// @public
function isNotNull(operand: any): boolean;

// @public
function isNotNumber(operand: any): boolean;

// @public
function isNotObject(operand: any): boolean;

// @public
function isNotRegExp(operand: any): boolean;

// @public
function isNotString(operand: any): boolean;

// @public
function isNotSymbol(operand: any): boolean;

// @public
function isNotUndefined(operand: any): boolean;

// @public
function isNull(operand: any): boolean;

// @public
function isNumber(operand: any): boolean;

// @public
function isObject(operand: any): boolean;

// @public
function isRegExp(operand: any): boolean;

// @public
function isSealed(o: any): boolean;

// @public
function isString(operand: any): boolean;

// @public
function isSymbol(operand: any): boolean;

// @public
function isUndefined(operand: any): boolean;

// @public
function keys(o: object): string[];

// @public
function keys(o: {}): string[];

declare namespace Lang {
    export {
        Throw,
        typeOf,
        typeOfEquel,
        instanceOf,
        throwError
    }
}
export { Lang }

// @public
const LN10: number;

// @public
const LN2: number;

// @public
function log(message?: any, ...optionalParams: any[]): void;

// @public
const LOG10E: number;

// @public
function log1p(x: number): number;

// @public
function log2(x: number): number;

// @public
const LOG2E: number;

// @public
function log_2(x: number): number;

declare namespace Math_2 {
    export {
        abs,
        acos,
        acosh,
        asin,
        asinh,
        atan,
        atan2,
        atanh,
        cbrt,
        ceil,
        clz32,
        cos,
        cosh,
        exp,
        expm1,
        floor,
        fround,
        hypot,
        imul,
        log_2 as log,
        log2,
        log1p,
        max,
        min,
        pow,
        random,
        round,
        sin,
        sinh,
        sqrt,
        tan,
        tanh,
        trunc,
        atan2xy,
        E,
        LN10,
        LN2,
        LOG2E,
        LOG10E,
        PI,
        SQRT1_2,
        SQRT2
    }
}
export { Math_2 as Math }

// @public
function max(...values: number[]): number;

// @public
function min(...values: number[]): number;

// @public
export function NOOP(): void;

declare namespace Not {
    export {
        isNotArray as Array,
        isNotBoolean as Boolean,
        isNotNaN as NaN,
        isNotNull as Null,
        isNotNumber as Number,
        isNotObject as Object,
        isNotUndefined as Undefined,
        isNotString as String,
        isNotFunction as Function,
        isNotSymbol as Symbol,
        isNotBigInt as BigInt,
        isNotDate as Date,
        isNotRegExp as RegExp,
        isNotArrayBuffer as ArrayBuffer
    }
}

declare namespace Object_2 {
    export {
        getPrototypeOf,
        getOwnPropertyDescriptor,
        getOwnPropertyNames,
        create,
        defineProperty,
        defineProperties,
        seal,
        freeze,
        preventExtensions,
        isSealed,
        isFrozen,
        isExtensible,
        keys,
        assign,
        getOwnPropertySymbols,
        is,
        setPrototypeOf,
        values,
        entries,
        getOwnPropertyDescriptors,
        fromEntries
    }
}
export { Object_2 as Object }

// @public
function ownKeys(target: object): (string | symbol)[];

// @public
const PI: number;

// @public
function pow(x: number, y: number): number;

// @public
function preventExtensions<T>(o: T): T;

// @public
function preventExtensions_2(target: object): boolean;

// @public
function random(): number;

declare namespace Reflect_2 {
    export {
        apply,
        construct,
        defineProperty_2 as defineProperty,
        deleteProperty,
        get,
        getOwnPropertyDescriptor_2 as getOwnPropertyDescriptor,
        getPrototypeOf_2 as getPrototypeOf,
        has,
        isExtensible_2 as isExtensible,
        ownKeys,
        preventExtensions_2 as preventExtensions,
        set,
        setPrototypeOf_2 as setPrototypeOf
    }
}
export { Reflect_2 as Reflect }

// @public
function round(x: number): number;

// @public
function seal<T>(o: T): T;

// @public
function set(target: object, propertyKey: PropertyKey, value: any, receiver?: any): boolean;

// @public
function setPrototypeOf(o: any, proto: object | null): any;

// @public
function setPrototypeOf_2(target: object, proto: object | null): boolean;

// @public
function sin(x: number): number;

// @public
function sinh(x: number): number;

// @public
function sqrt(x: number): number;

// @public
const SQRT1_2: number;

// @public
const SQRT2: number;

// @public
function tan(x: number): number;

// @public
function tanh(x: number): number;

declare namespace Throw {
    export {
        throwError as Error,
        throwEvalError as EvalError,
        throwRangeError as RangeError,
        throwReferenceError as ReferenceError,
        throwSyntaxError as SyntaxError,
        throwTypeError as TypeError,
        throwURIError as URIError
    }
}

// @public
function throwError(message: string, ErrorType?: Function): unknown;

// @public
function throwEvalError(message: string): unknown;

// @public
function throwRangeError(message: string): unknown;

// @public
function throwReferenceError(message: string): unknown;

// @public
function throwSyntaxError(message: string): unknown;

// @public
function throwTypeError(message: string): unknown;

// @public
function throwURIError(message: string): unknown;

// @public
function trunc(x: number): number;

declare namespace Type {
    export {
        Is,
        Not,
        isString,
        isBoolean,
        isNumber,
        isArray,
        isObject,
        isNull,
        isUndefined,
        isNaN_2 as isNaN,
        isFunction,
        isSymbol,
        isBigInt,
        isDate,
        isRegExp,
        isArrayBuffer
    }
}
export { Type }

// @public
function typeOf(operand: any): string;

// @public
function typeOfEquel(operand: any, type: string): boolean;

// @public
function values<T>(o: { [s: string]: T } | ArrayLike<T>): T[];

// @public
function values(o: {}): any[];

// @public
function warn(message?: any, ...optionalParams: any[]): void;

```
