/**
 * Create a new RegExp object
 * @param {string} pattern - The text of the regular expression
 * @param {string} flags - includes `i`, `g`, `d`, `m`, `s`, `u`, `y`
 */
export function NewRegExp(pattern: string, flags: string): RegExp;