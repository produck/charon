/**
 * determines whether a operand is null or not
 * @memberof Lang.Type
 * @function isNull
 * @param {operand} operand
 * @returns {Boolean}
 */
 export const isNull = (operand) => {
	return operand === null;
}